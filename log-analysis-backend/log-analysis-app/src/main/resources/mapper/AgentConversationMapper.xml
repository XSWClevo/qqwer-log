<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.mw.loganalysis.agent.mapper.AgentConversationMapper">

    <update id="ensureTable">
        CREATE TABLE IF NOT EXISTS agent_conversations (
            id VARCHAR(64) PRIMARY KEY,
            user_id BIGINT NOT NULL,
            title VARCHAR(255) NOT NULL,
            preview TEXT,
            datasource_id VARCHAR(36),
            datasource_name VARCHAR(255),
            datasource_type VARCHAR(50),
            message_count INTEGER NOT NULL DEFAULT 0,
            created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
            updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
            last_message_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP
        )
    </update>

    <update id="ensureUserLastMessageIndex">
        CREATE INDEX IF NOT EXISTS idx_agent_conversations_user_last_message
        ON agent_conversations(user_id, last_message_at DESC)
    </update>

    <update id="ensureUserUpdatedAtIndex">
        CREATE INDEX IF NOT EXISTS idx_agent_conversations_user_updated_at
        ON agent_conversations(user_id, updated_at DESC)
    </update>

    <select id="selectRecentByUserId" resultType="cn.mw.loganalysis.agent.entity.AgentConversation">
        SELECT id,
               user_id,
               title,
               preview,
               datasource_id,
               datasource_name,
               datasource_type,
               message_count,
               created_at,
               updated_at,
               last_message_at
        FROM agent_conversations
        WHERE user_id = #{userId}
        ORDER BY last_message_at DESC, updated_at DESC
        LIMIT 100
    </select>

    <select id="selectOwnedConversation" resultType="cn.mw.loganalysis.agent.entity.AgentConversation">
        SELECT id,
               user_id,
               title,
               preview,
               datasource_id,
               datasource_name,
               datasource_type,
               message_count,
               created_at,
               updated_at,
               last_message_at
        FROM agent_conversations
        WHERE user_id = #{userId}
          AND id = #{sessionId}
        LIMIT 1
    </select>

    <insert id="insertIfAbsent">
        INSERT INTO agent_conversations (
            id, user_id, title, preview, datasource_id, datasource_name, datasource_type,
            message_count, created_at, updated_at, last_message_at
        )
        VALUES (
            #{sessionId}, #{userId}, #{title}, '', #{datasourceId}, #{datasourceName}, #{datasourceType},
            0, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP, CURRENT_TIMESTAMP
        )
        ON CONFLICT (id) DO NOTHING
    </insert>

    <update id="updateAfterTurn">
        UPDATE agent_conversations
        SET title = CASE
                        WHEN message_count = 0 OR title IS NULL OR title = '' OR title = '新对话'
                            THEN #{title}
                        ELSE title
                    END,
            preview = #{preview},
            datasource_id = COALESCE(#{datasourceId}, datasource_id),
            datasource_name = COALESCE(#{datasourceName}, datasource_name),
            datasource_type = COALESCE(#{datasourceType}, datasource_type),
            message_count = message_count + #{addedMessages},
            updated_at = CURRENT_TIMESTAMP,
            last_message_at = CURRENT_TIMESTAMP
        WHERE id = #{sessionId}
    </update>

    <delete id="deleteOwnedConversation">
        DELETE FROM agent_conversations
        WHERE user_id = #{userId}
          AND id = #{sessionId}
    </delete>

</mapper>
