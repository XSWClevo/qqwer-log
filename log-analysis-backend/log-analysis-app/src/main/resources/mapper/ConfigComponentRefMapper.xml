<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.mw.loganalysis.vector.mapper.ConfigComponentRefMapper">

    <select id="selectSharedComponentsByConfigId" resultType="cn.mw.loganalysis.vector.entity.SharedComponent">
        SELECT sc.*
        FROM vector_shared_components sc
        JOIN vector_config_component_refs cr ON sc.id = cr.shared_component_id
        WHERE cr.config_id = #{configId}
    </select>

</mapper>
