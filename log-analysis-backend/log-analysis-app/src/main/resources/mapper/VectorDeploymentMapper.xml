<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.mw.loganalysis.vector.mapper.VectorDeploymentMapper">

    <select id="selectByMachineId" resultType="cn.mw.loganalysis.vector.entity.VectorDeployment">
        SELECT *
        FROM vector_deployments
        WHERE machine_id = #{machineId}
        ORDER BY created_at DESC
    </select>

    <select id="selectByConfigId" resultType="cn.mw.loganalysis.vector.entity.VectorDeployment">
        SELECT *
        FROM vector_deployments
        WHERE config_id = #{configId}
        ORDER BY created_at DESC
    </select>

    <select id="selectByConfigVersion" resultType="cn.mw.loganalysis.vector.entity.VectorDeployment">
        SELECT *
        FROM vector_deployments
        WHERE config_version = #{configVersion}
        ORDER BY created_at DESC
    </select>

    <select id="selectLatestPendingByMachineId" resultType="cn.mw.loganalysis.vector.entity.VectorDeployment">
        SELECT *
        FROM vector_deployments
        WHERE machine_id = #{machineId}
          AND status = 'pending'
        ORDER BY created_at DESC
        LIMIT 1
    </select>

    <select id="selectPageByCondition" resultType="cn.mw.loganalysis.vector.entity.VectorDeployment">
        SELECT *
        FROM vector_deployments
        <where>
            <if test="machineId != null and machineId != ''">
                AND machine_id = #{machineId}
            </if>
            <if test="configId != null and configId != ''">
                AND config_id = #{configId}
            </if>
            <if test="status != null and status != ''">
                AND status = #{status}
            </if>
        </where>
        ORDER BY created_at DESC
    </select>

</mapper>
